// type is more like an struct, maybe?
type Employee = {
    name: string,
    startDate: Date;
    //(work: string): boolean;
}

// type is more like an struct, maybe?
type Manager = {
    name: string,
    privileges: string[]
}